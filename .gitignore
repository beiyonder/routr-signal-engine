# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.eggs/
*.egg-info/
*.egg
dist/
build/

# uv
.uv/
uv.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Env / secrets
.env
.env.local
*.secret

# Runtime
*.log
.cache/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Local-only digest scratch (committed digests live under data/digests/)
scratch/

# Runtime cache files containing credentials (reddit token, twitter cookies in
# any shape: flat-dict json, playwright list json, base64 export, twikit sqlite).
# Be aggressive: anything matching twitter-cookies* is off-limits.
data/cache/reddit-oauth.json
data/cache/reddit-device-id.txt
data/cache/twitter-cookies.json
data/cache/twitter-cookies-playwright.json
data/cache/twitter-cookies-b64.txt
data/cache/twitter-cookies*
data/cache/twitter-session.db
data/cache/playwright-profile/
# Anchor embeddings are public and SHOULD be committed (data/cache/anchors-*.json).

# NOTHING under data/ is committed to the public repo. Pipeline state moves
# through Actions cache + artifacts now. Everything below is for local-only
# inspection; the workflow on CI never touches the public git history.
data/digests/
data/raw/
data/seen/
data/leads/
data/intel.db
data/intel.db-shm
data/intel.db-wal

# Internal strategy + handoff + brainstorm docs (keep code public, strategy private)
_handoff/
conversation/
PLAN_v2.md
prompt.md
scratch/

# Manual Discord paste-ins may include third-party content \u2014 author chooses what to push
data/manual/
